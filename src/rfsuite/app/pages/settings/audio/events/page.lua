local M = {}

-- Events is a tile menu of one page per category. home.lua renders the submenu from the
-- manifest when a page defines no build(), the way Settings > Audio above it is rendered;
-- category_page.lua beside this file builds every one of those pages.

return M
